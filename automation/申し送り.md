# 申し送り・未処理タスク（マチノワ記事自動生成）

最終更新: 2026-08-28
セッションが切れても失われないよう、**会話の中だけにあった情報をここに落としている**。
作業前に `automation/記事URL運用ルール.md` と合わせて必ず読む。

---

## A. 未処理タスク（着手されていない）

### A-1. クラウドルーティン3本の削除（ユーザー操作が必要）
`machinowa-auto-10jst` / `15jst` / `20jst` は **enabled だが実行セッション0件＝完全な空振り**
（RemoteTrigger API の `list_runs` が空。`last_fired_at` だけ更新されるので動いて見える）。
Gmail通知も 2026-07-11 を最後に途絶。実働はローカル launchd の 08:00 のみ。

- 削除は https://claude.ai/code/routines から（Claude 側は enabled:false の停止までしかできない）
- **3本のプロンプトにサービスアカウントの秘密鍵が base64 平文で埋まっている**
  （machinowa-bot@machinowa-automation）。削除後も**鍵のローテーション推奨**

### A-2. ローカル20時便の登録（Claude の権限では実行不可）
夕方トスアップ分が翌朝8時まで着手されない問題の解消。ユーザーが実行する。

```bash
cp ~/Library/LaunchAgents/com.machinowa.auto.08jst.plist ~/Library/LaunchAgents/com.machinowa.auto.20jst.plist && sed -i '' -e 's/08jst/20jst/g' -e 's|<integer>8</integer>|<integer>20</integer>|' ~/Library/LaunchAgents/com.machinowa.auto.20jst.plist && launchctl load ~/Library/LaunchAgents/com.machinowa.auto.20jst.plist && launchctl list | grep machinowa
```

### A-3. 真っ黒画像30店舗の是正
hero.jpg と point2.jpg が **同一の真っ黒プレースホルダ**（md5 `006e6f2ac3de777dfa9a15da0c7e4de8` / 20,558 bytes）。
Google のフォールバック画像で、**「20KB以上」の検証を78バイト差ですり抜ける**。

対象30店（`public/restaurants/teleapo-*`）:
BetterDays / CAFEDEFLEUR / CafeFuu / USHIWAKAMARU / WHITEBASECAFE / cafebarGroovy /
filmcafebar / kanekocoffeebeans旭川 / vegeeggLABO / ぎゃらりー喫茶なよたけ / オッティモ /
クッチーナウッチーノ / トラットリアネロnero / ナナンテアnananthea / メモアール /
ライブカフェNobodyKnows / リストランテドラマティコ / 備後ピザ / 割烹なり田 / 大和 /
天や椀や / 岡崎天然昆布水つけ麺礼音 / 忍者喫茶小田原道michi / 炉端と地酒まる文すが原 /
炭焼き日本料理三ツ井 / 紅谷食堂 / 酒場むゆう / 銀しゃも鶏焼肉玄 / 鎌倉由比ヶ浜蕎麦slove / 馳走かねこ

- 全30本 **noindex**（品質ゲートが弾いている）。ただし **URLは生きており営業先が開ける**
- 発生源はクラウドrouting ではなく**過去の手動・バッチ生成コミット**
  （`feat: 特集記事13本追加（新シート 残り全件）` など。うち2件は
  `feat(teleapo): 画像が存在しない2店の記事を公開` と、画像が無いと分かった上で公開）
- **再発防止として、サイズ閾値ではなく md5 ブラックリスト＋色分散判定が必要**（未実装）

### A-4. 推測表現を含む記事のリライト
機械検出で **推測表現 50本 / 100回**（〜だと思う・のだろう・かもしれない 等）。
ほかに 情報不足の自白1本・失礼表現2本・根拠なし断定3本。

重症ワースト:
`ルーラル`(9) / `OWL`(6) / `小料理木下`(6) / `Ristorantet.v.b`(5) / `大阪福島鮨ふみ`(5) /
`和牛焼肉たつ竹`(5) / `炭やよつ葉`(4)

`ルーラル` は「詳細な業態情報は限られているが」と自白した上で1記事まるごと想像で構成。
**クラウドrouting が生成した9店**（ルーラル/OWL/炭や。よつ葉/社交酒場イム/あんばい食楽厨房/
点心厨房桃花/炭火家本舗なんばや/久留米晩餐-Bansun-/テスト）に、記録済みの
**場所ハルシネ事故5件が集中**している（クラウドからGoogle Mapsに到達できず推測で書くため）。

---

## B. パイプラインの現在の全ステップ（`automation/local-pipeline.sh`）

```
Step -1  実行前ヘルスチェック（ネットワーク / claude CLI 認証）
Step 0   処理済み台帳(generated-ledger.json)の再構築
Step 0.5 本体スプシの 済/URL 再同期     ← ビューには一切書かない(2026-08-27に削除)
Step 0.6 記事台帳シートの同期
Step 1   候補抽出 (scripts/sheets-candidates-json.mjs = 本体を読む)
Step 2〜 候補ごとに: ロック → Maps解決 → 画像取得 → 記事生成 → build → 禁止語 →
         commit/push → スプシ書き戻し
最終     vercel --prod → 本番200確認
最終     sync-article-ledger.mjs --apply → link-ledger-urls.mjs（D列を日本語表示＋リンク化）
最終     check-gaps.mjs（成果物ベースの抜け検知。抜けがあればデスクトップ通知）
```

## C. 候補検出スクリプトの違い（混同禁止）

| スクリプト | 読む先 | 行番号 | 用途 |
|---|---|---|---|
| `scripts/sheets-candidates.mjs` | **詰めOKリスト(ビュー)** | ビューの行番号 | 人が見る表示用 |
| `scripts/sheets-candidates-json.mjs` | **トスアップ元シート(本体)** | **本体の行番号** | **パイプラインが使う正** |

ビュー側の行番号で本体に書き戻すと**別の店を上書きする**。書き込みは必ず本体行番号で。

---

## D. ログを信じるな（実証済みの罠）

**集計値も成功/失敗ログも嘘をつく。判断は「記事が実在するか」で行う。**

1. `未処理 0件` … 24時間クールダウン中の失敗が候補から消えるため。実際に3回取りこぼした
2. `完了サマリ: 成功2 / エラー1` … claude が完了マーカーを出さず
   `Waiting on the Vercel deploy to propagate.` で終了しただけで、記事は正常に作成済みだった
   （2026-08-28 焼肉真壁）
3. `Background tasks still running after 600s; terminating` … 環境起因のタイムアウト。
   店舗側の問題ではないので `--status=clear` で再投入すれば通る

→ 唯一信用できるのは `node scripts/check-gaps.mjs`

---

## E. 今回直した不具合（再発したら真っ先に疑う）

| 不具合 | 原因 | 対処 | commit |
|---|---|---|---|
| URL末尾に `**` が付き本番500 | claudeの出力文から正規表現でURLを拾っていた | SAFE_NAME を正とする | f7ea406 |
| 毎朝W/X列が復活し二重表示 | sheets-sync-status.mjs がビューのW/Xを再構築していた | ビューへの書き込みを全削除 | 4bba57a |
| `--force` が効かない | `--force`(値なし)が空文字=falsy | 存在すれば真として扱う | (mark-done) |
| D列がリンクにならない | 平文の日本語URLはSheetsが自動リンク化しない | リッチテキストリンクを埋め込む | db80edd |

---

## F. 現在の状態（2026-08-28 時点・実測）

- 詰めOK **88件 / 記事あり 88件 / 抜け 0**
- 記事台帳D列 88行すべて「日本語表示＋クリック可＋数式でない」
- 詰めOKリストの残骸列 0セル
- 掲載記事 `lib/teleapo-features.ts` に約140本、うち index対象は `lib/teleapoIndexable.ts`
- Search Console 実測(7/12〜8/8): クリック151 / 表示8,151 / CTR1.85% / 平均掲載順位11.6
  → CTR改善（店舗ページのtitle/description）が最も伸びしろが大きい。未着手
